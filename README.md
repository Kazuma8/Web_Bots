# Web_Bots

## Dependencies
Selenium (the others should be preinstalled with Python)

##Instructions
Run the program above. 

There is a known issue where the Configuration times out (or takes infinite time). In this case, try changing the browser that Selenium uses to drive the page. 

